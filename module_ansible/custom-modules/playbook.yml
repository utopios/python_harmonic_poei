---
- hosts: all
  become: true
  tasks:
    - name: custom module
      first_custom_module:
        name: "value of name argument"
        description: "description value"
      register: output_module
    - name: debug
      debug:
        var: output_module