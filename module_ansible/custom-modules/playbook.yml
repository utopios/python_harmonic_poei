---
- hosts: all
  become: true
  tasks:
    - name: custom module
      first_custom_module:
        name: "value of name argument"
        description: "description value"
      register: result
    - name: debug
      debug:
        var: result